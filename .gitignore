# Build output — regenerated by every build, never tracked
[Bb]in/
[Oo]bj/
.vs/
*.csproj.user

# Heavy local archive — stays on disk, too large for git (~780 MB: v1.0 source + build, v2.0/v3.0 release zips)
Releases/

# Windows Explorer thumbnail cache
Thumbs.db

# AI PM — the machine-local file and per-session scratch, never tracked
CLAUDE.local.md
docs/ai-pm/local/
docs/ai-pm/usage-detail/
docs/ai-pm/dashboard.html
