# Random Creation — Changelog

Append-only, newest on top. From the AI PM 5 upgrade (2026-09-12) every entry heads `## YYYY-MM-DD — headline`; the project-version headers that grouped entries until versions were retired at 4.0 now ride the headline in parentheses. An entry locates the record; it never is the record. Pre-adoption history (app v1.0 → v2.0 → v3.0) was backfilled at the v3.3 close-out at milestone granularity — see the final section, and note that its dates are stated only to the precision the surviving sources support.

## 2026-09-20 — AI PM upgrade 7.0.1 → 8.1.0

The survey fired no row of 8's checklist: all twelve entry headings already in the charter's shape, no tool registration under docs/ai-pm/tools/, no old copy, no local edits. CONFIG (docs/ai-pm/config.md): nothing touched; 8 changes no setting's meaning or value. THE VERSION FILE (docs/ai-pm/version): 8.1.0, written by the execute and carried by the shape commit. HANDOFF: the annotation line at its top; neither exception fired, the Until further notice section holding None and no program in flight. INDEX: read for rows stating a fact the major changed; none, the governance table naming the AI PM skills without names or count, so nothing corrected. MACHINE: `setup skills --check` reports the user-level skills current under 8, the new `ai-pm-post` skill already on the machine, so no refresh. What 8 brings that this project may use from here: `/ai-pm-post` drafts an overview, release, update or numbers post from the record and hands over text to paste, nothing filed; a program's phase may be held; a tool registration's step may declare its own timeout. Skeleton: nothing missing. No renames, no moves, no ignore lines. Plan items struck: none. Detail: commit "AI PM 8.1.0 — brought into shape" and this commit.

## 2026-09-18 — AI PM upgrade 6.1.0 → 7.0.1

The second major of the day, run straight after the first; a project skips no major. CONFIG (docs/ai-pm/config.md): Project type: software build added, settled by the source tree without a question, and Handoff: on added, the software build's default and how the project has always run, both at the head of the settings block with their definitions; the Feedback Pass definition reworded to 7's text, which names `/ai-pm-self-check` and `/ai-pm-feedback`; the six existing values unchanged. ORIENTATION RECORD: §5 renamed from "Technical environment" to 7's "Environment and sources" in the table row and the heading, the confirmed sentence unchanged. HANDOFF: the annotation line at its top; neither exception fired. INDEX: read for rows stating a fact the major changed; none, the governance table naming the AI PM skills without names or count, so nothing corrected. MACHINE: the user-level skills refreshed once to 7's names, ten skills with the `ai-pm-` prefix laid and the five old-name folders (closeout, closeout-auto, charter-review, feedback-pass, self-check) removed by the installer; from here the close-out is `/ai-pm-closeout` or `/ai-pm-closeout-auto`, the feedback report `/ai-pm-feedback`, and a setting changes through `/ai-pm-settings`. Skeleton: nothing missing. No tool registrations, no renames, no moves, no ignore lines. Plan items struck: none. Detail: commit "AI PM 7.0.1 — brought into shape" and this commit.

## 2026-09-18 — AI PM upgrade 5.14.1 → 6.1.0

The version file read 5.13.0; 5.14.1 was the minor served at this session's start. CONFIG (docs/ai-pm/config.md): Dashboard: on and Started Ceiling: 3 added with their definitions; the Usage definition gains "no rebuild" and the Allowed write locations definition names each registered tool's clone; Feedback Pass on, Push follow remote, Allowed write locations none and Usage on unchanged. THE BACKLOG: docs/backlog.md converted into the folder docs/backlog/, one file per item, eight items written by the backlog module in one manifest after a walk of the set: four post-release chores set CURRENT by ruling, in the old file's order (Download-back verification of the v4.0 release; The live user's upgrade to v4.0; Write the v4.0 design record; Retire the local Releases folder), three parked ideas PROPOSED with their triggers (Updater; Remaining v4.0 screenshots; v3.0-era refactor and feature candidates, kept as one item until a candidate is decided), one standing rule ONGOING (Workflow helper actions on a newer Node.js); the old file retired by the conversion. No roadmap existed; no program. NEW: docs/design/project-orientation.md, §1 The application and §5 Technical environment seeded from the record and confirmed by the developer, the other sections left as open questions; its index row after the last LIVE row and a read-first line in CLAUDE.md's top pointer paragraph. CLAUDE.md: the identity line made a heading (`# Random Creation — WPF desktop app (C# / .NET 8)`), the working-title line folded into the App line to stay under the ceiling, 199 lines. HANDOFF: the upgrade annotation at its top; NEXT SESSION shrunk to the next move alone, the queue now the folder's; nothing else rewritten. INDEX: the docs/backlog.md document row replaced by a docs/backlog/ row under Where things live; the design row names the orientation. REGISTER: the two standing conditions' "(backlog, Planned)" and "(backlog, Ongoing)" references now name their items. MACHINE: the settings check under 6 found the three hooks and four rules present, so no paste; the nine user-level AI PM skills re-laid by `setup skills`, the once-per-machine step the notes warn of. No tool registrations, no ignore lines, no renames, no moves. Plan items struck: none. Detail: commit "AI PM 6.1.0 — brought into shape" and this commit.

## 2026-09-12 — Product page live, README brought to v4.0

Prompted by a friend wanting to try the app: the GitHub Releases page was judged unfit for a non-technical visitor (developer toolbar, downloads below a collapsed Assets heading beside two source archives), so the Proposed product page was built and shipped in one session. THE SITE: `site/index.html` and `site/style.css`, plain HTML and CSS in the app's own look; one Download for Windows button whose link, version and size come from the GitHub Releases API at page load with the latest-release page as fallback; three-step explanation, features, the five v4.0 screenshots, installing both ways, the SmartScreen warning explained, common questions. Verified locally at desktop and phone width, dark and light, both download links resolving. THE DEPLOY: `.github/workflows/pages.yml` publishes `site/` on any change to it on main; Pages enabled on the repository with GitHub Actions as its source, and the repository's About link set to the page, both at the developer's "go live"; the first run's failure email predates the enablement and the rerun succeeded. README: Getting it leads with the product page and describes both download forms; Your content names both data locations; the sample note reflects the conditional copy; Status reads 4.0. Work commits 8b418ac (site and workflow), f27e5ce (README), 0906bbd (browser launch config). Detail: the environment register's Product page row, the lifecycle guide's GitHub section, and this commit.

## 2026-09-12 — AI PM upgrade 4.0 → 5.13.0

The method left the project: the eight 4.0 method files under Documents\AI PM\ and the five .claude\commands\ triggers deleted (no local edits since the 4.0 upgrade commit, so no feedback report was owed); the session-start hook now serves the method from the machine's shelf clone. CONFIG: moved to docs/ai-pm/config.md; Feedback Pass on, Push follow remote and Allowed write locations none carried unchanged; Usage: on added; the Project line retired into CLAUDE.md's identity line and the External storage map line into the index. THE TREE: Documents\ became docs/; the cadence files renamed (handoff.md, backlog.md, changelog.md, index.md); the records sorted into kinds under kebab names (design: 2026-06-06-project-context-v3.0.md, 2026-08-01-release-plan-v4.0.md, project-context-v1.0.md, project-context-v2.0.md; reference: engineering-notes.md, file-index-v3.0.md; guides: development-lifecycle.md); icons and screenshots to docs/assets/; the pre-git snapshots to docs/archive/. NEW: docs/environment.md (toolchain, GitHub, pipeline, two standing conditions), docs/ai-pm/version, CLAUDE.local.template.md, .gitattributes with the usage log union-merged, four ignore lines for AI PM's local files; the two transit ignore lines removed. RESHAPED: the index to its three sections, absorbing CLAUDE.md's two tables, the lifecycle guide's where-things-live table and the old doc index; the handoff to three sections; the backlog to four (the parked items to Proposed with their triggers, the action-version bump to Ongoing); this changelog's entries to dated headings with the retired project versions in parentheses, words unchanged; CLAUDE.md from 239 to 194 lines with the path-free routing block, environment facts to the register and status to the handoff. The v3.0 project context's state set to FROZEN. The four live records' cross-references updated to the new names; the README's five screenshot links repointed. Plan items struck: none. Detail: commit c30b37d ("AI PM 5.13.0 — brought into shape") and this commit.

## 2026-08-07 — CLAUDE.md pointer-block typo fixed

the AI PM block's close-out line read \closeout and \closeout-auto with backslashes; now /closeout and /closeout-auto, matching the canonical 4.0 block. Two characters, introduced at the same day's 3.2 → 4.0 upgrade, requested by the developer. Detail: this commit.

## 2026-08-07 — AI PM UPGRADED 3.2 → 4.0, the method's redesign release

Method files replaced by Manifest: the rationale doc, instructions and README replaced; four procedure docs (Closeout, Upgrade, Release, Feedback) and the Manifest added; the Feedback Protocol and /closeout-light retired (light mode is gone; /closeout and /closeout-auto remain). Config migrated in place: project versions retired (commits now lead with their species, not a version number), the Record docs field moved out to the new Documents\DOC_INDEX.md, and Push: follow remote added — this project has a remote, so every commit the AI makes now pushes immediately, aligning with the standing push-early ruling. Project files at the upgrade survey: Status_and_Backlog.md became Backlog.md, forward-looking only (no stored status — summaries generate from this changelog on request); CLAUDE.md got the 4.0 pointer block and shed the "How we work together" section, which now lives at the developer's user-level CLAUDE.md; the handoff's four riders routed out (two were already backlog items, the UI-automation caveat went to the lifecycle doc §3, and the mid-session-commit friction finally left as a feedback report to the maintainer); per-doc version stamps removed from the lifecycle doc, engineering notes and v4.0 release plan — git is the history. Drift check clean: no method file had been locally edited. Detail: this commit ("AI PM upgrade 3.2 → 4.0").

## 2026-08-03 — Repository moved to the akcama GitHub organization (project v3.5)

The transfer itself was done by the developer on GitHub; this session verified the new repository (same tip commit ddfd808 as local before switching), pointed the local remote origin at https://github.com/akcama/random-creation.git, and confirmed fetch and push both work against the new address. A reference sweep (commit 36b489d, made mid-session at the developer's approval) updated the old henry-akcama/random-creation address in eight files: CLAUDE.md (which now also records the move and the redirect), the release workflow's release-notes link, the installer's publisher fields (AppPublisher renamed henry-akcama → akcama), the in-app GitHub link in DataService.cs (build verified; takes effect next release), and the lifecycle, handoff, status and AI PM config docs. Deliberately unchanged: this changelog's v3.2 entry, which accurately records creation under henry-akcama, and the commit noreply identity, which is account-level. GitHub redirects the old address, so nothing published breaks. Detail: commit 36b489d and this close-out commit.

## 2026-08-02 — v4.0 BUILT AND SHIPPED, in one session, eleven work commits (1785acf…6a394a2) plus this close-out (project v3.4)

Every item verified in the running app before its commit, per the plan's verify-after-each rule. THE FIXES: BUG 1, the v3.0 themes moved into Themes\ and proven by before/after screenshots and pixel sampling (the visible symptom was the Manage Content group-header tint); BUG 3, WM_GETMINMAXINFO answered with the current monitor's work area — verified pixel-exact on both monitors — plus a follow-up squaring the window corners while maximised; BUG 2 by removal, MigrationDialog and the V2*/Legacy* models deleted, startup reduced to three cases (fresh/current/back-up-and-notify), tested against the genuine v2.0 data set; BUG 4, printing rebuilt around one shared page builder for preview and printer with real pagination. THE CHANGES: portable.txt marker forks user data between data\ beside the exe and %LocalAppData%\RandomCreation (CHANGE via lifecycle §6); Open data folder button (4); conditional sample install that can never overwrite content (5); purpose-built three-collection sample — Creature/Starship/Swords (6); changelog.txt relocated to a program file beside the exe (7); serial numbers from an ever-climbing counter in settings, shown everywhere a result appears (1); compact print header, darkened greys, and a Page-N-of-M footer on every page (2); dimmed items print again at 60% (3); five README screenshots reshot into Screen Shots\v4.0\, Presets slot swapped for Collections with the developer's approval (8). PACKAGING: Inno Setup script (per-user, no UAC, uninstaller defaults to keeping content) at Source\RandomCreation\Installer\; tag-driven release pipeline at .github\workflows\release.yml with a dry-run mode, which passed first try including the installer's first compile. THE RELEASE: developer tested installer, portable, uninstaller and a real proof print (settling the two open print questions on paper); generation counter reset to 0 by ruling before shipping; assembly bumped to 4.0.0.0; tag v4.0 pushed and the release published at 20:58 UTC with both downloads. Detail: the eleven commits and the release plan; EngineeringNotes' migration section rewritten in-session as the plan required. Note for the record: work landed in per-item commits rather than riding the close-out commit — the project's push-early-is-backup ruling and the developer's verify-per-item review model, applied deliberately.

## 2026-08-01 — GitHub finished, Visual Studio fixed, and v4.0 planned end to end (project v3.3)

THE PUSH SEQUENCE, executed in order because order was load-bearing: the 10 pre-adoption commits rewritten from the real address to GitHub's noreply alias via git filter-branch — safe only while nothing had been pushed — then verified three ways (all 11 commits on the noreply address; tree hash f39cd91e identical before and after, so no file content moved; GitHub's tip hash matching local, so the remote provably holds the rewritten history), then the first push with Git Credential Manager authorising once through the browser, then refs/original deleted and the old objects garbage-collected. The real address now exists nowhere in the repository. VISUAL STUDIO: F5 was failing on a "Just My Code" warning because the build configuration had been left on Release; switching to Debug resolved it, and the command-line build was confirmed to agree. The stray .vs folder at the project root was deleted on an explicit ruling, git-ignored files having no safety net. THE PLANNING PASS produced twelve items with their fix approaches already decided — four of them found by reading rather than reported: printing silently discards everything past one page while the scrolling preview faithfully shows it all; a fresh install announces a v2.0 migration that never happened, and the same unreachable condition makes the v1.0 recovery path dead code; the print header is built twice, which would have let a corrected preview ship alongside an uncorrected printout; and changelog.txt sits on the wrong side of the program-versus-data line the installer forces. THE INSTALLER DESIGN PASS was held as the standing ruling required, settling per-user install, Inno Setup, %LocalAppData%, both installer and portable shipping self-contained, a marker file to tell them apart, and an uninstaller defaulting to keeping user content. The app's next release is declared 4.0 — note it now diverges from the project version, which continues its own line. Detail: this commit, Documents\Design\RandomCreation_ReleasePlan_v4_0.md (new, authoritative for what v4.0 contains and why), and RandomCreation_DevelopmentLifecycle.md v1.1. Records: RandomCreation_ProjectContext_3_0.md renamed to RandomCreation_EngineeringNotes.md after a comparison proved it was never redundant — none of its content appears in the full v3.0 record — with two obsolete sections cut, one of which instructed the reader to put categories.json in the data folder.

## 2026-08-01 — Project structure and storage design, taught rather than handed over, then executed (project v3.2)

The session's design thinking became a new record doc, Documents\Design\RandomCreation_DevelopmentLifecycle.md v1.0 — authoritative for how this project is worked and shipped (the "can Build recreate it?" storage test, the build cycle, why push and release are different acts, tags, the deliberate no-branches decision, licensing, the portable-vs-installed fork, updater options, sample content). docs\ retired: its v1.0/v2.0 snapshots moved to Documents\Archive\ after being proven the ONLY surviving copies of pre-git source, and its remaining 36 files deleted after hash-verification against Source\ (docs\v3.0\ differed only in whitespace, byte-order marks and one trailing newline). categories.json homed at Source\...\SampleData\ and wired in the .csproj to ship to samples\ rather than data\, so a release can never overwrite a user's content; build verified clean with an existing data\ folder left untouched. GitHub set up: repository henry-akcama/random-creation created public and connected as origin, LICENSE (all rights reserved, explicitly not open source) and README.md written, commit identity switched to GitHub's noreply alias globally. Backlog gains the installable-build item with a standing ruling that it gets a detailed design pass before any building. Push and commit-rewrite deferred to immediately after this close-out. Detail: this commit and the lifecycle doc.

## 2026-08-01 — File organization pass and CLAUDE.md rework, in eight commits (06888ff…a315c82) (project v3.1)

The Claude-project export zip was extracted into docs\ and hash-verified against the archive copies; .gitignore scoped to exclude build output, Releases\ and Sharable EXE\; design records and all screenshots consolidated into Documents\Design\; icon assets consolidated into Documents\Design\Icons\ with verified-identical duplicates removed; the v1.0 build moved from Sharable EXE\ to Releases\Random Creation V1.0\Creature Crafter\, retiring that root folder; the Source Code\ tree retired entirely after every file was hash-proven redundant or, for 63 phase snapshots, superseded by shipped v3.0 and retained in git. Root went from eight folders to five. CLAUDE.md rewritten from a 12-line stub to the full project guide, merging docs\CLAUDE.md and porting three working-style sections from the IFQ Tracker reference file, which was then retired. Two findings: the shipped v3.0 loads v2.0 theme dictionaries (BUG 1), and the .NET SDK installed with Visual Studio builds this project from the command line in ~7 seconds, so the AI can build and run the app directly. Detail: this commit and the eight it follows. NOTE: the commit hashes cited above were rewritten at v3.3; the equivalent commits are now 3f64189…98ee378.

## 2026-07-31 — AI PM 3.2 adoption (project v3.0)

Method installed and configured: git repository initialized (branch main) on the existing v3.0 project, config filled (Feedback Pass on, no external storage, no allowed write locations), transit folders created and git-ignored, backlog/changelog/handoff born, adoption guide removed. Project version negotiated at 3.0 to match the shipped app. Detail: this commit.

---

### Pre-adoption history — the app before AI PM

Backfilled at the v3.3 close-out at milestone granularity, per AI_Project_Manager.md §12. These are APP versions, predating both git and the method, so there are no commits behind them. They locate the record rather than being it: the app's own user-facing changelog at Source\RandomCreation\RandomCreation\changelog.txt, the frozen design records in Documents\Design\, and the pre-git source snapshots in Documents\Archive\.

#### App v3.0 — June 2026, built 2026-06-06

The Groups release, and much the largest of the three. A CategoryGroup layer was inserted between Collections and Categories, making the hierarchy Collection → Group → Category → Option, with existing data migrated automatically on first launch. Manage Content was rebuilt around groups with a new group detail panel; weight tiers expanded to six; inline editing, undo (Ctrl+Z), an internal copy/cut/paste clipboard, Duplicate (Ctrl+D) and Ctrl+drag copy arrived; results became grouped cards; and Mark as Drawn, the AI Prompt feature, print preview, rebuilt presets, Collections Management, data export, a keyboard shortcuts reference and toast notifications all landed. The model classes lost their Random prefix. Detail: changelog.txt "Version 3.0" section, 22 headings; RandomCreation_ProjectContext_v3_0.md remains authoritative for the design. The build date comes from the release exe's timestamp — no dated source states it. This is the version still running on the one live installation, and the version whose shipped build loads v2.0 theme dictionaries: the defect recorded as BUG 1 and fixed in v4.0.

#### App v2.0 — May 2026

Renamed from Creature Crafter to Random Creation. Collections were introduced above categories, giving the app more than one independent content set for the first time. Manage Content became its own screen; presets, themes, per-option enable/disable with probability weighting, result card dimming, rebuilt drag and drop, history improvements, a settings screen, a resizable sidebar, a summary bar and the custom borderless window all landed. The save file moved out of a single JSON beside the exe and into the data\ folder that survives to this day. Detail: changelog.txt "Version 2.0" section, 13 headings; RandomCreation_ProjectContext_v2.0.md. Source: Documents\Archive\v2.0\, the only surviving copy.

#### App v1.0 — date not established

Shipped as Creature Crafter, the original working title. A single flat list of categories, each holding options; Generate picked one option per enabled category and displayed the combination. C# / WPF on .NET 8, System.Text.Json, no NuGet packages — the constraints the project still runs under today. Data lived in creature_crafter_data.json beside the executable, saved on every change and deliberately excluded from the single-file publish so users could back it up or transfer it. Already documented at the time as a general-purpose combination generator rather than a creature-specific one, a position CLAUDE.md still enforces. NO DATE IS RECORDED and none is inferred: changelog.txt has no v1.0 section, the context doc is undated, and the archived source files all carry 2026-07-30 extraction timestamps rather than original ones. Only that it precedes v2.0's May 2026 is established. Detail: RandomCreation_ProjectContext_v1.0.md; source in Documents\Archive\v1.0\.
